/**
 * Represents a single item in the select list.
 */
export type SelectListItem = {
  /**
   * The unique value for the option, used for identification.
   */
  value: string;
  /**
   * The display text for the option shown to the user.
   */
  label: string;
};
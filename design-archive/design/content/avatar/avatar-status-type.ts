/**
 * Defines the possible status states for an Avatar, used to display a visual indicator.
 * 'online' - typically green.
 * 'offline' - typically grey.
 * 'away' - typically yellow or orange.
 * 'busy' - typically red.
 */
export type AvatarStatus = 'online' | 'offline' | 'away' | 'busy';
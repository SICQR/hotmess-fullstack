import AgeGateModal from '@/components/AgeGateModal';

export default function XXXHub(){
  return (
    <div className="space-y-6">
      <AgeGateModal />
      <h1 className="text-5xl" style={{fontFamily:'var(--font-anton)'}}>XXX — 18+</h1>
      <p>Enter to view adult content. Consent and age verification required.</p>
    </div>
  );
}
